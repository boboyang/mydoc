useradd
userdel -r test

#create passward
passwd

-group:
    groups: show groups
    groupadd
    groupdel

grppasswd

sudo:
    su -: change to root
    sudo -i: temp use root permission, load root profile

root:
    visudo: edit /etc/sudoers   # sudo securities
