#job control
&       #backgroup run
^Z      #stop
jobs
fg 
bg
kill:
    kill -0 PID     # test if a process exist
    kill -l
         1) SIGHUP
         2) SIGINT:  like ^+C
        15) SIGTERM
    man 7 signal

#PID and PPID  # parent PID
ps -l          #view all
ps aux
pstree Aup

nohup   # run even log-off
top
echo $$

w           #cpu load
free -m     #memory usage

vmstat 

sar
    -f filename # reading logfile
    -r          # check memory
    e.g.:
        sar -f /var/log/sa/sa13 #date 13
        sar -r -f /var/log/sa/sa13

