#http://vbird.dic.ksu.edu.tw/linux_basic/0320bash.php#variable
#variable usage

name=whille
unset name
echo $name #or ${name}
name="while's name"
# or name=name=while\'s\ name  #while's name

#append
name=${name}" is me"

#export variable to use in sub shell
export name=XXX
sh
echo $name
exit

# `XXX` execute first
#example:
ls /lib/modules/$(uname -r)/kernel

# show all env
env

#本 shell 的 PID
echo $$

# return value
echo $?

locale

#shell in shell
sh
echo $$
sh
echo $$
exit
exit

#interact with user
read -p "please input" -t 10 name

declare -i sum=1+2+3 # as number
echo $sum
declare -r sum # set read only
declare -p sum # show info

#variable del
#{var#key}          #from head, min match
#{var##key}         #from head, max match
%,%%                #from tail
#replace
#{var/old/new}      #repl first
#{var//old/new}     #repl all

new_var=${old_var-default}
