#分类：
secure txt: ssh
UI:         VNC

public  key:    用来加密
private key:    用来解密
因为网路连线是双向的，所以，每个人应该都要有对方的『公钥』才对。
双方都有自己的私匙和对方的公匙，所以整个叫非对称式金钥系统。
client的key每次都随机生成

#server:
sudo apt-get install openssh-server
ps -e |grep ssh  	#check if started

StrictModes no
PubkeyAuthentication yes
AuthorizedKeysFile      .ssh/authorized_keys

sudo /etc/init.d/ssh restart

#default conf
/etc/ssh/sshd_config
#set this to local config, to improve connection speed
GSSAPIAuthentication no

/etc/ssh/ssh_host*

#client:
~/.ssh/known_hosts

sftp
scp     #ssh copy

＃创建SSH密匙
ssh-keygen -C 'whille@163.com' -t rsa

#scdc server 
ssh whillewang@scdc.mydlink.com -p 2226
#client
scp -P 2226 ~/.ssh/id_rsa.pub whillewang@scdc.mydlink.com:~/

＃提交密匙 到github
https://github.com/accYount/ssh
选择Add another public key。Title随便取，Key是一段东西。
找到刚才创建密匙的那个目录下找到id_rsa.pub文件，把它打开可以看到一堆文字，拷贝下来黏贴到github页面key的空白处。然后Apply，就好了。

＃查看网络服务
cat /etc/services

# update projects to server

cd env
scp -r mydlink_v3 root@172.18.195.214:env/mydlink_v3
scp -r servicesmock root@172.18.195.214:env/servicesmock

#SCDC
ssh -L 8901:localhost:8901 -p 2226 whillewang@scdc.mydlink.com
#scp -P 2226 ~/.ssh/id_rsa.pub whillewang@scdc.mydlink.com:temp_key


#rsync 112
rsync -avz -e ssh ./ root@172.18.195.112:/root/fake-sigd

ssh bsdc@60.251.172.83 -p 33220
pw:bsdc@2012

#beijing
ssh root@124.205.163.89 -p 1210
pw:mydlink01

#tw
ssh bsdc@60.251.172.83 -p 33220 (gateway and syslog-ng client)
ssh root@10.32.100.193
ssh root@10.32.100.195  (syslog-ng server)

syslog-ng -V

