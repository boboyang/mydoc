#start server
/usr/local/syslog-ng/sbin/syslog-ng -F -v -d -t

# start client
/home/mongodb-linux-x86_64-2.0.2/bin/mongo
#show dbs
use signalingd      # db name
#show collections    # similar to SQL tables

#auto create collection: pardata
#db.pardata.insert({name : 'Min', age : 20})
db.pardata.find()
db.pardata.drop()    # remove table

db.dropDatabase()   # remove db

